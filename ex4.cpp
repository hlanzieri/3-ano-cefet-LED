#include <bits/stdc++.h>

using namespace std;

int validarLinha()

int main (){
    ifstream fin("labirinto.txt");

    if(fin.is_open()){
        // Passos para validar o labirinto
        // 1) Validar o tamanho das linhas e colunas
        // 2) validar se quando i == 1 || i == N || j == 1 || j == N o caractere eh "#"
        // 3) Validar se quando eh diferente dos acima sao "."

    }
    return 0;
}